# 服务器监听的端口号
config.serverPort=18050
config.bossCount=1
config.workerCount=50
config.keepalive=true
config.backlog=100
# 采集任务每 2 秒钟执行 下发帧
config.heartbeat=2
# 设备 连接到服务器端后，延时执行定时任务
config.delay=35
# 采集周期
config.readPeriod=15

